'use strict';

let config = {
	paths: {
		tempDirectory: './tmp'
	},
	//defaultGrammarPath:  './scratch/cardimg-only-grammar.json',
	// defaultGrammarPath:  './scratch/test-grammar.json'
	// origin: "#gathererRulingImage#",
	//origin: "#gatherer#",	
	// origin: "#headlineKeepOrMulligan#"
	origin: "#mtgComic#"	
};

module.exports = config;